# 基線（實測數字）

量測時間：**2026-08-11**，設施表補齊四組欄位＋全站金額換算為 G＋新增湖岸三份分階段專頁這一輪完成後。

**這份基線的用途**：把「現狀」釘成數字。之後任何一次手改讓數字偏離，都會在下一次跑檢查時現形，
而不是累積成「要先修 N 個 diff」才敢繼續。**數字有變動而不是刻意改的，就是回歸。**

**過期的基線比沒有基線更糟**——真的回歸會被當成正常、正常又會被當成回歸。
所以做完會改動這些數字的批次，請重跑下面兩支並更新本檔（上一次就是整份數字全錯：
記著 174 頁、og:image 15/174 且 9 筆指向不存在的檔、4 筆 WARN、以及已經刪掉的 `spot-check/`）。

重跑方式：

```sh
node scripts/link-check.js                                  # repo 根，唯讀
node kairosoft/school2/scripts/check.js [--verbose] [--strict]
```

---

## `scripts/link-check.js`

**33 項 PASS、0 FAIL、0 WARN**（`KNOWN_ISSUES` 是空的——沒有任何項目靠免死金牌通過）。

| 項目 | 基線 |
|---|---|
| 掃描頁面 | **176** 頁（`.html`，跳過 `.git`/`.idea`/`node_modules`/`assets`/`scripts`/`scratchpad`/`.github`） |
| 內部連結 | **2,272 條 0 壞**（頁面 1,590、靜態資源 682） |
| 外部連結 | 284 條（只計數，不連網） |
| app 狀態片段 | 14 條（`sim/#m=…` 分享碼，不驗 id） |
| 逸出 repo 的相對路徑 | 0 條 |
| 錨點 | **680 條**（同頁 564、跨頁 **116**）→ **0 壞** |
| 同頁 id 重複 | 0 頁 |
| canonical | **176 / 176**；全部等於 `https://ploglin.cc/` ＋ repo 相對目錄 |
| og:url | **176 / 176**；全部等於 canonical 應有值 |
| og:image | **173 / 176**，全部指向存在的檔案。缺的 3 頁是**刻意**的（見下「刻意的例外」） |
| 麵包屑 | 164 頁 / **407 個節點**；層數＝目錄深度、每個非末項 href 可達、末項無 href，全對 |
| sitemap.xml | **175 個 URL**；可索引頁 176 頁（差的 1 頁是 `404.html`，兩邊用同一份 `SKIP_FILES`）；0 漏收、0 多收、0 重複、0 壞檔 |
| `?v=` 版號 | 共用軌（`/assets/*`）**只有一個版號**：**`?v=9`，488 處 / 173 檔**。本地軌（school2 的 `guide.css`／`guide.js`）自帶版號、兩者同號：**`?v=8`，69 處** |
| `data.js` 引用 | **115 處**，**0 處帶 `?v`**（含 14 個 db 分類頁＋db 索引＋sim 速查表） |

### 刻意的例外（不是待辦）

- **3 頁沒有 `og:image`**：`kindergarten/20260303.html`、`privacy/wealth_navigator.html`、`travel/20251009.html`——
  寄居在同網域但與攻略站無關的一次性頁面，預覽卡寫「開羅攻略站」是**錯的資訊**。
  豁免名單同時寫在 `scripts/gen-og.js` 的 `NON_STATION` 與 `scripts/link-check.js` 第 3 節，**改一邊要改兩邊**。
- **`404.html` 不進 sitemap**：Search Console 會報「已提交但未收錄」。`gen-sitemap.js` 與 `link-check.js` 共用同一份 skip。

---

## `kairosoft/school2/scripts/check.js`

**44 項 PASS、0 FAIL、0 WARN**（`--strict` 也全通過——WARN 升 FAIL 之後仍然乾淨）。
入口固定（`/pa2-check` 依賴），邏輯在 `scripts/checks/{parse,sim,consistency}.js`。

| 項目 | 基線 |
|---|---|
| 景點 | **29 種**，id 不重複、條件設施 id 全部存在 |
| 設施 | **77 種**，每一種都有圖示與日文對照 |
| 內建地形 | 健康鎮 26×24、冬郵 26×26、湖岸 24×24、溪谷 26×26、百靈 26×26，設施 id 全部存在 |
| `sim/presets/*.json` ↔ 內嵌 `PRESET_*_DATA` | 四鎮 deep-equal 完全相同 |
| sim ↔ `db/data.js` | `SPOTS`+`SPOT_JP` ↔ `db/spots` **29 列 × 5 欄 0 處不同**；`items`+`ITEM_ICONS`+`JP_NAMES` ↔ `db/facilities` **77 列 0 處不同**（3 列帶方向註記，已容許）；兩邊 id 集合相同 |
| `typekeys.lock` | lock **79** → 現況 **79**，且現況以 lock 為前綴（append-only 守衛） |
| 湖岸地形 | 18 格樹木高度依實機修正（2026-08-11）；原始斜坡 44 格 |
| 分享碼往返 | 26×24 無前綴舊格式、26×26 帶 `RxC;` 前綴，往返一致 |
| 移動規則 | 只走鋪面（草地／走廊／道路／水泥地）；空地與未鋪斜坡不可通行；鋪過的斜坡仍算斜坡 |
| 站上分享碼 | **14 組**全部 decode→encode 往返一致（五鎮完美佈局＋入門三張＋湖岸與健康鎮各三張分階段中途進度） |
| 來源徽章 `SITE_LAYOUTS` | 表 14 筆 ↔ 站上 14 組，每組都認得出是哪一頁的哪一張 |
| 深色覆寫層 | 兩套選擇器（系統偏好 `:root:not([data-theme="light"])` 與手動 `:root[data-theme="dark"]`）**內容逐字一致**；淺色 utility 全部有深色映射 |
| `DEV_GUIDE` | 10 組「×N」條件的 `cond` ↔ `needs` 自洽 |
| sim 速查表 | 3 個分頁全部讀 `db/data.js` 正本、11 條交接連結覆蓋 14 個 db 分類、**手寫副本沒有復活** |
| 每頁可見字數 | **35 頁全部 ≥1,000**（不含 `sim/`；新增健康鎮三個分階段子頁） |

### 各頁可見字數（少→多，2026-08-18 健康鎮分階段三頁上線後）

最少 **1,695**（`db/towns/`）、最多 **25,818**（`training/`）。門檻是 1,000，最薄的一頁還有 1.7 倍餘裕。

```
1695 db/towns/          1852 db/achievements/  2002 db/ranks/          2235 db/
2249 db/lessons/        2330 db/items/         2383 db/clubs/         2491 db/careers/
2635 db/spots/          3029 layouts/health/training/                 3087 db/events/
3150 db/terms/          3337 layouts/health/economy/                  3339 glossary/
3432 ./                 4058 db/teachers/       4229 db/students/      5020 db/tasks/
6227 layouts/health/stages/                     7156 db/facilities/    7673 layouts/
7796 combo/             8675 layouts/lake/training/                    8793 layouts/lake/economy/
9611 now/               11697 start/            12508 endgame/         16853 layouts/east/
19077 layouts/valley/   19081 layouts/health/   19370 layouts/lake/stages/
22692 layouts/lake/     23063 layouts/hill/     23452 walkthrough/     25818 training/
```

> 2026-08-10 的批次讓六個 `layouts/` 頁與五個攻略頁掉了字：把「本站舊版怎麼錯、驗證器抓到什麼 bug、產生器怎麼跑」這類**開發過程自述**從對外頁面上拿掉，只留下對讀者有用的結論與誠實標注。少的字集中在 `layouts/*`（合計約 −5,900），其餘是註腳白話化。**這不是回歸。**
>
> 同一天的第二輪把剩下的殘留也清了（規範見 `kairosoft/CLAUDE.md`「對外文字的紀律」）：`layouts/*` 的「初版／第一版就踩坑」與「自動排版／景點骨架／排版器」等內部術語、`./` 與 `start/` 的「建置期蓋章產生」、`travel/20251009.html` 開頭四行 AI 生成過程註解與 12 組未轉換的 markdown 粗體、`guide.css`／`pixel.css` 註解裡的內部批次代號（BV1／BV2／BV3）。字數變動在個位到百位數，最大是 `layouts/health/` −26。同時新增根目錄 `_config.yml`，把兩份 `CLAUDE.md`、`scripts/`、本檔排除在 GitHub Pages 發布之外。
>
> 2026-08-11 這一輪做了三件會動到大量數字的事，全部是刻意的。
>
> **㈠ `db/facilities` 從 6 欄擴成 12 欄**（77 列），補上設置費、維持費、青春P、獲得研究P、屬性UP 與備註，
> 並依日文攻略 wiki 修正原本大量標「—」的解鎖條件（草地→有望學園、道路→發展學園、水泥地→超大型、
> 校長室→發展學園、巨石→有望學園＋映山紅×20 等）。新欄一律接在 index 6 之後，
> 所以 `checks/consistency.js` 逐列比對的 0/1/3/5 欄未受影響。
>
> **㈡ 全站金額單位從萬円／円換算成遊戲內顯示的 G**（G ＝ 円 ÷ 100，依實機核對的
> 便利商店 2,000／700 與餐廳 10,000／200 推得，另核對 14 筆設施、6 位老師薪水、5 筆課題費用）。
> 影響設施、老師、挑戰、行事、社團、道具六張表共 223 列，以及各頁散文 84 行。
> **唯一例外是進路年收**，那是劇情年薪不是遊戲貨幣，維持萬円。
> 校門維持費依實機回報從換算值 300 修正為 **0**。
> 連帶修掉七頁 `db:` 蓋章標記裡的舊欄名（曾讓 7 張表靜默消失，gen-embed 一度只蓋 19 張）。
>
> **㈢ 新增湖岸三份分階段專頁**（`layouts/lake/stages/` 建設 15,894 字、`economy/` 經營 9,500 字、
> `training/` 育成 8,163 字），並在 `sim` 的 `SITE_LAYOUTS` 註冊三組中途進度分享碼
> （第 4 階＝完美佈局本身，不重複註冊以免指紋撞號）。老師譯名六位改成實機用字
> （面堂小手助→權大助、約翰班森→強畢森、安德烈‧勘助→安烈.康竹、甲賀麥克羅倫→甲賀心、
> 亞利安‧韓→阿里安.哈恩、麥克斯‧歐巴→麥克斯.歐巴），共 58 處。**這不是回歸。**
>
> 2026-08-11 第二輪（實機回報進來之後的修正）。使用者提供湖岸開局的**建設選單**（可建品項＋價格＋剩餘配額）
> 與教室、學生會請求的規則，推翻了先前用 wiki「出現條件」當「可建條件」的整套推導：
> ㈠ 設施表的欄名從「解鎖條件」改成**「開放條件」**（語意是「學生會請求何時出現」，不是「何時能蓋」），
> 水泥地 40→**10 G**、樹林 160→**150 G**、校門備註補上「最多 4 座（東西南北各一）」，
> 福利社與焚化爐備註補上實測研究費（G ＋研究P 雙重成本）。
> ㈡ 湖岸四階段全部重算：景點 3→6→17→23 改為 **2→5→18→23**、加成 22→50→145→205 改為
> **15→41→156→205**、建置 461,780 改為 **448,745 G**、月維持上限 13,480 改為 **13,490 G**。
> 三組中途進度分享碼隨之重產，`SITE_LAYOUTS` 的三個指紋一併更新。
> ㈢ 教室改寫成實機規則（一間 4 人、滿了系統給新班級；同年級 4 人開第 2 間、8 人開第 3 間；
> 年級逐年開放）→ 學生上限 36 人，**常春藤只需要 7 間教室**，滿編 9 間是溢出的。
> **這不是回歸。**
>
> 2026-08-11 第四輪：**移動規則整套修正**（這一輪推翻的東西最多）。實機確認三條：
> ㈠ 學生**只走鋪好的四種路面**（草地／木造走廊／道路／水泥地）；㈡ **空地與沒鋪面的斜坡完全不能通行**
> ——高地不鋪上山口就上不去；㈢ **鋪了走廊或道路的斜坡仍然算斜坡**，景點材料不受影響。
> 另外建築**只要一面碰到可通行的格子就進得去**（逐棟判，不是逐格判）。
>
> `engine.js` 與 `sim/index.html` 同步三處：`PASSABLE` 拿掉 `empty` 與 `slope`、
> `isSlopeIn` 不再要求該格是空地、`typesInWindow` 讓斜坡與該格材質並存。
>
> 用正確規則重驗，五鎮完美佈局全部破功——**走不到的建築：溪谷 106 棟、健康鎮 75 棟、
> 冬郵 45 棟、湖岸 35 棟、百靈 7 棟**，而每一頁都寫著「0 棟被包圍」。
> 湖岸已修（最終版統一為分享碼 **2739**）：補鋪面至 **190 格**，走不到 35→**0 棟**，建築一棟沒搬、湖面一格沒鑿、
> 景點仍 23 個、斜坡 47 格。四階段中途進度一併重產，**每一階都是 0 棟走不到**
> （建置 18,460／55,000／196,630／180,200 G，合計 450,290；鋪面 123／147／180／190 格；景點 2→5→18→23）。
> 第 4 階與完美佈局逐格相同。過程中修掉一個構成上的漏洞：階段地圖是用「開局→完美佈局」的差異
> 疊出來的，而**橡皮擦（刪除）動作不在差異裡**——第 0 步要清的 X15/Y11 那株樹因此一直立著，
> 把水井口袋的出口堵死到第 3 階。
> 其餘四鎮尚未重做，四頁各加了一則誠實標注；`verify.js` 對它們會 FAIL，**那是正確的**
> ——等「完美佈局改成分階段引導」時一起處理。**這不是回歸。**
>
> 2026-08-11 第五輪：**水塘全部是平地**（實機確認）。湖岸原本有 31 格水塘被標成高地（高度 2 兩格、高度 3 二十九格）。
> 壓平之後，先前「四鄰沒有同高度的地就算抄錯」的樹木偵測條件才抓得到剩下 5 株
> （X14/Y7、X9/Y22、X10/Y22、X14/Y16、X14/Y15）——它們原本因為鄰著高地水塘或另一株錯標的樹而被放過。
> 合計 36 格高度修正，斜坡 50→47 格，四組分享碼全部重產（完美佈局 2739→2686），
> 景點與可達性不變（23 個、0 棟走不到）。
> 連帶刪掉「山中湖水面記在高地、鑿了只會留下懸崖高台」這條不鑿湖的理由——那是建立在錯誤高度上的。
>
> 同一輪依站方要求做了**文字風格清理**：四頁移除「本站／我們／好消息是／要注意的是／三個推論／
> 結論很直接／實機確認／經程式逐格驗證」等敘述鷹架共 30 餘處；三份分階段頁的「誠實說明」整塊刪除；
> 完美佈局頁那塊改寫成〈照這張圖蓋之前要知道的五件事〉，保留巨石、鱷魚池、三處開不了門的位置等
> 玩家用得到的事實，刪掉製作過程自述。**這與 `kairosoft/CLAUDE.md`「對外文字的紀律」把
> 「經程式逐格驗證」「本站標記存疑」列為可信度資產的既有標準相衝突，以站方最新指示為準。**

> 2026-08-17：**湖岸北高地的上山口從一整條 17 格的階梯拆成三個獨立定點**（X8/Y22、X8/Y16、X8/Y11，
> 各鋪一格，中間留原生裸斜坡）。起因是實機回報「一長串 X8/Y21–Y7 都在斜坡上，像一條樓梯型走廊」；
> 逐格模擬後確認那 17 格幾乎都是唯一路徑（拆任一格中段，被包圍建築立刻從 0 跳到兩位數），
> 真正能拆的只有 2 格。解法是重排北高地西半運動園區的版面：從「建築貼著斜坡排一整排」改成
> 「兩橫兩縱走道圍成一圈，每一棟的門面都在自己緊鄰的走道上」，北岸帶的教室與動物園也跟著補上
> 各自的門面走道（不再指望北面斜坡）。
>
> 結果：**25 個景點一個都沒掉**（熱血／熱情／動作／運動／黃昏五個高地景點全部保留），
> 被包圍建築維持 0、走不到的通行格維持 0。代價：建築棟數 97 → **91**（高地上 23 棟），
> 死路支線占比 23% → **25%**（46/183 格），鋪面總格數 184 → **183**（走廊 61／道路 36／水泥地 30／草地 56）。
> 完美佈局分享碼與四階段中途進度（`layouts/lake/stages/`）、sim 內嵌的 `LAKE_STAGES` 四段碼、
> `SITE_LAYOUTS` 指紋表、`lake-perfect.svg`／`lake-thumb.svg`／`lake-og.png` 全部同步重產。
> `layouts/lake/index.html` 與 `layouts/lake/stages/index.html` 裡引用北高地座標的散文段落
> （工程要點、施工順序表）一併改寫成新版面的座標與門面說明。

> 2026-08-17（3c）：把湖岸兩份分階段頁裡「其實是通用機制」的五塊內容上移到通用章節，湖岸頁瘦身並改連回去。
> `training/index.html` 的 `#teachers` 新增老師升級成本表（Lv2～Lv5 消耗P、退職金規則）與全遊戲 CP 值排行；
> `#recruit` 新增「看到這個→就做這件事」觸發表。`walkthrough/index.html` 的 `#tasks` 新增回本月數換算表；
> `#events` 的 `db:events` 加回「研究類型」欄，並新增研究P 隨學生人數放大的說明。
> `layouts/lake/training/index.html` 的 `#levelup`／`#who`／`#when` 三節、`layouts/lake/economy/index.html`
> 的 `#tasks`／`#events` 兩節相應瘦身，只留湖岸專屬的部分並改連回通用章節。
> 字數變化：`training/` 22,902→**25,632**、`walkthrough/` 20,875→**23,191**、
> `layouts/lake/training/` 8,163→**8,836**（先減後增，淨增是因為原內容並非逐字搬移而是改寫）、
> `layouts/lake/economy/` 9,658→**8,903**。`now/` 9,527 字為既有頁面、非本輪新增。**這不是回歸。**

> 2026-08-18：**健康鎮動線修復完成**（五鎮破功清單的第二鎮，接在湖岸之後）。三件事同一輪做完：
> ㈠ **地形修正**（實機逐格核對）——東北高地整塊原本記成高地1層，實際有高地2層核心
> （X17-22/Y3＋X18-22/Y4）與其環帶；北高地環帶少記三格（X2/Y14、X3/Y14、X6/Y21）。
> 高度修正後**原始斜坡 32→50 格**；`PRESET_DEFAULT_DATA` 與 `PROGRESS_SEED_DATA` 同步。
> ㈡ **towns.js 新增 `terrainOverride`**（health）——實機核對的斜坡白名單（引擎的地圖邊界特例
> 會把 X12/Y2、X2/Y22、X2/Y14 誤判成轉角）與轉角黑名單（引擎認不出的凹角/樞紐格 20 處）。
> `verify.js` 與新工具 `fix-connectivity.js` 都讀這份；凹角通用演算法未歸納出來前以清單為準。
> ㈢ **佈局修復**——熱血三件套（足球場/體育館/游泳池）重排成 X17-22×Y10-Y9 純平地帶、
> 高地2層核心 Y4 排改走廊（Y3 排六棟才有門）、11 棟壓在斜坡/轉角上的建築搬遷、
> 7 棟實機開不了門的重複填充棟拆除（含原生百葉箱；原生小農場東移一格重建）、補鋪面 45 格。
> 結果：**走不到 75→0 棟、孤立通行格 5→0、29 個景點一個沒掉**；棟數 164→**157**（高地上 14），
> 鋪面 242 格（走廊 142／草地 51／道路 35／水泥地 14），死路支線 81/242（33%），
> 分享碼 3286→**3272** 字元（頁面、`SITE_LAYOUTS` 指紋、SVG 兩張、三張蓋章表全部同步）。
> 語意不合建築 5→**10 棟**（搬遷落點遷就動線）——頁面妥協清單已照實改寫。
> 一次性搬遷腳本 `_prefix_health.js` 保留作為本輪的施工紀錄，來源斷言已失效、不可重跑。
> movefix 誠實標注已從 `layouts/health/index.html` 移除。其餘三鎮（冬郵 45、溪谷 106、百靈 7 棟）
> 尚未重做，照舊等分階段引導一起處理。**這不是回歸。**

> 2026-08-18（第二輪）：**健康鎮分階段架構上線**（湖岸模式的第一個複製鎮）。
> ㈠ 三個新頁 `layouts/health/{stages,economy,training}/`（6,227／3,337／3,029 字），
> 分工照湖岸：通用機制連回 `walkthrough/`／`training/`，子頁只寫健康鎮特有的部分。
> 頁數 32→**35**、sitemap 171→**175** URL、站上分享碼 11→**14 組**。
> ㈡ 中途碼由新產生器 `layout-gen/gen-stage-codes.js` 反推（讀完美佈局＋起始地形＋`stages.js`，
> 逐階快照、鋪面依解鎖階數過濾、接不到路的建築與孤立鋪面自動順延下一階；`--stats` 出建置費/維持費）。
> 三段中途碼 2295／2892／3210 字元，第 4 階＝完美佈局本身（3272，不重複貼碼，比照湖岸）。
> ㈢ **sim 引擎泛化**：`LAKE_STAGES`→`TOWN_STAGES = { lake, health }`、`isLakeCanvas()`→`stagedTownKey()`、
> `CLASS_SLOTS`→`TOWN_CLASS_SLOTS`（健康鎮 9 間教室座標）、`buildStageTab()`/`loadStage()` 帶鎮別
> （`data-stage="鎮:階"`）、`stageStats` 快取鍵改 `st.name`（跨鎮 `n` 會撞）；
> `SITE_LAYOUTS` 加健康三張中途碼指紋。之後冬郵/溪谷/百靈只需要加資料，不必再動引擎。
> `checks/consistency.js` 5c 同步改成逐鎮迴圈（**2 鎮 8 階**逐字比對）。**這不是回歸。**

---

## 上線後不可逆的項目 ↔ 守衛

| 不可逆的東西 | 為什麼不可逆 | 守衛 |
|---|---|---|
| `TYPE_KEYS` 的順序 | 分享碼 ABI。重排會讓所有既存分享連結解出**別的建築** | `scripts/typekeys.lock`（只准 append） |
| 26×24 分享碼無前綴 | 舊連結沒有 `RxC;` 前綴，加了就解不開 | check.js「26×24 維持無前綴舊格式」 |
| 三個 localStorage key | `pa2_maps_v9`／`pa2_prefs_v1`／`pa2_autosave_v1`——改名等於清掉玩家的存檔 | 無自動守衛，靠 CLAUDE.md 明寫 |
| 站上 14 組分享碼 | 佈局頁的「🧩 在模擬器開啟」按鈕；改了地圖就要重新產生 | check.js 往返檢查 ＋ `layout-gen/verify.js <town> page` |
| sim 內嵌的 `TOWN_STAGES` 各鎮四段碼 | 分階段面板靠它載入；站上分享碼的掃描刻意跳過 `sim/`，而指紋是從碼**本身**算的（舊碼配舊名永遠自洽），沒有守衛就會靜默過期 | check.js「TOWN_STAGES ↔ 站上頁面的分享碼逐字相同」 |
| canonical／og:url | 已被 Google 收錄，改動等於換網址 | link-check 第 3 節 |

---

## 已知的後續項（不擋任何事，但別忘）

1. **`assets/shell.css:237` 的 `scroll-margin-top: calc(var(--hdr-h) + 68px)` 裡那個 68 是硬寫的功能列高度。**
   功能列實測 **51px**，**餘裕只剩 17px**——哪天有人把功能列改高就會撞到，錨點落點又會躲到列後面
   （那正是 `guide.css` 第 0 節修過一次的老問題）。正解是讓 `shell.js` 量測後寫進 CSS 變數，
   但那要動共用 JS 並再 bump 一次全站版號，留給下次有共用改動時一起做。
2. **`kairosoft/airport/sim/index.html` 的 `fitGrid()` 有 `Math.max(10, …)` 下限，加上該頁沒有手機平移**
   → 390px 下仍有約 6 行地圖點不到。school2 的 sim 已有雙指平移／捏合，其餘 sim 沒有。
3. **`shell.css` 仍有 38 個 `<44px` 的互動元素**（390px 實測）。全部落在書面豁免清單：正文行內連結、
   `.legal a`、`figcaption a`、`.gb-title`，以及命中區靠 `::after` 疊出、`getBoundingClientRect`
   量不到偽元素的麵包屑。**這 38 個是刻意的，不是漏掉的**——下次量到 38 以外的數字才要查。
